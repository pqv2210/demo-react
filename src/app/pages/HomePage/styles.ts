import * as React from "react"

export const WrapPage: React.CSSProperties = {
  alignItems: "center",
  justifyContent: "center",
  display: "flex",
  width: "100%",
}

export const WrapContent: React.CSSProperties = {
  backgroundColor: "#F5F7FC",
  padding: 50,
  borderRadius: 8,
}
